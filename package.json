{
  "name": "airrnr_be",
  "version": "1.0.0",
  "description": "express backend for airrnr app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "dev": "NODE_ENV=development.local nodemon -r dotenv/config server dotenv_config_path=/home/alvingohhm/projects/airrnr_be/config/.env.development.local",
    "local": "NODE_ENV=local nodemon -r dotenv/config server dotenv_config_path=/home/alvingohhm/projects/airrnr_be/config/.env.local"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "mongoose": "^6.1.6"
  },
  "devDependencies": {
    "dotenv": "^11.0.0",
    "nodemon": "^2.0.15"
  }
}
